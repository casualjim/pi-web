---
"@jmfederico/pi-web": patch
---

Ship Terminal as a required bundled browser/server plugin while preserving its panel, navigation, PTY, replay, reconnect, command-run, and selected-machine workflows. Add revision-paired, workspace-scoped plugin requests and bounded duplex channels, with explicit safe-start recovery when the required Terminal entry cannot activate.
